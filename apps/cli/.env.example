# ============================================================================
# Find My Domain - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# All CLI options can be configured via environment variables
#
# Priority order: CLI args > input.json > .env > defaults
# ============================================================================

# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-api-key-here

# Output directory for results
FMD_DIRECTORY=output

# TLDs to check (comma-separated)
FMD_TLDS=com,io,dev,ai

# Example domains for inspiration (comma-separated)
FMD_DOMAINS=example1.com,example2.io

# Keywords for domain generation (comma-separated)
FMD_KEYWORDS=tech,startup,innovation

# Number of domains to generate
FMD_COUNT=10

# AI model to use
FMD_MODEL=gpt-4o-mini

# Custom prompt template (use {COUNT}, {DOMAINS}, {KEYWORDS} placeholders)
# FMD_PROMPT=Generate {COUNT} creative tech domain names

# Save results to file (true/false)
FMD_SAVE=true

# Enable streaming mode (true/false)
FMD_STREAM=true
